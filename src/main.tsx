import React from 'react'
import ReactDOM from 'react-dom/client'
import AgileSearch from './AgileSearch'
import './index.css'

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <AgileSearch />
  </React.StrictMode>
)
